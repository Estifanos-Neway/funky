// const { adminsDb } = require("../database");

module.exports = {
    // signIn: async ({ username, passwordHash }) => {
    //     const adminExists = await adminsDb.exists({ conditions: { username, passwordHash } });
    //     if (adminExists) {

    //     }
    // }
};